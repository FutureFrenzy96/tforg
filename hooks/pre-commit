#!/bin/sh
# Pre-commit hook: format and organize staged Terraform files. Aborts the
# commit when files are rewritten so the changes can be reviewed and
# re-staged. Install with `tforg install-hook`, or copy this file into
# .git/hooks/pre-commit and chmod +x. Requires tforg on PATH.
exec tforg -staged
